using Entities;
using Services;

namespace Pegi.Api.Test;

public class StudentTest
{
   /* private StudentsService? _studentsService;
    private Student _student;


    [SetUp]
    public void SetUp()
    {
        _studentsService = new StudentsService();
    }

    [Test]
    public void SaveUserTest()
    {
        _student = new Student();
        _student.Document = "a";
        _student.AmountCredits = "a";

        var response = _studentsService.SaveStudent();
        Assert.That(response, Is.EqualTo(true));
    } */
}
